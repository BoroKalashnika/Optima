import {
    View,
    StyleSheet,
} from 'react-native';

const Buscar = (props) => {
    return (
        <View style={styles.container}>
        </View>
    );
}

const styles = StyleSheet.create({

});

export default Buscar;