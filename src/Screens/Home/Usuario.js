import {
    View,
    StyleSheet,
} from 'react-native';

const Usuario = (props) => {
    return (
        <View style={styles.container}>
        </View>
    );
}

const styles = StyleSheet.create({

});

export default Usuario;